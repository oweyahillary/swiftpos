# Normalise everything to LF in the repository.
# Working copies stay CRLF on Windows; git stores LF.
* text=auto eol=lf

# Windows-only scripts must keep CRLF on disk
*.bat text eol=crlf
*.cmd text eol=crlf

# Binaries — never touch
*.png binary
*.ico binary
*.xlsx binary
*.zip binary
