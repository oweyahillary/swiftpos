# SwiftPOS — Session handoff, 2026-08-28

**The session in one line:** root-caused and fixed "sales read *synced* on the till
but never reach the cloud." It was never one bug — it was a chain (a sync deadlock,
invisible failures, a bad expense id, a batch that 500'd whole, and finally the real
culprit: order-number collisions between tills that the client swallowed as
success). All fixed; the ~KES 12,510 of lost 26–27 Aug sales recovered; and the
collision made **structurally impossible** on the cloud.

Base of all this work: commit **`d5bd396`** (A167–A174). The user applied patches
incrementally on their Windows box and pushed as they went.

---

## STATE RIGHT NOW (read this first)

- **Cloud DB (prod):** migrations **93 and 94 are APPLIED and live** (ran through
  `scripts/migrate.mjs`, recorded in `schema_migration_runs`). Verified:
  `orders_biz_branch_device_ordernum_uidx` exists. **The order-number collision is
  now impossible in prod.**
- **Server (Render) + dashboard (Vercel):** the user says **deployed**.
- **Desktop:** **rebuilt** to the A182 build. Current live till device is
  **`55e8dd9f`** (v0.5.38), terminal code should be set **distinct** (T1 = old dead
  till `f77f63d7`, T2 = the recovered orders; use **T3** for the new one).
- **Repo:** the user's working tree has the code from patches `-m`, `-n`, `-o`
  (staged but **not yet committed** as of session end). **A183's files are only
  PARTLY in the tree** — see "UNFINISHED" below.

## UNFINISHED — the one thing to close the commit

The `-p` patch (A183) failed to apply on the user's box (register drift +
`migrations/94_...sql` already existed), so in the repo:
- ✅ `migrations/94_order_number_per_device.sql` — the user must **recreate this file
  by hand** (it's live in the DB but not in the tree). Body:
  ```sql
  ALTER TABLE public.orders DROP CONSTRAINT IF EXISTS orders_business_id_branch_id_order_number_key;
  CREATE UNIQUE INDEX IF NOT EXISTS orders_biz_branch_device_ordernum_uidx
    ON public.orders (business_id, branch_id, COALESCE(device_id, ''::text), order_number);
  ```
- ⚠️ Missing too (documentation/tests, optional): `docs/MANIFEST-2026-08-27-p.md`,
  `tests/order-number-per-device.test.mjs`.

**Before committing:** run `node scripts/migrate.mjs --plan` — it must say
**pending 0**. If it shows `94` pending, the runner logged it under a different name
than the file; reconcile before it tries to re-run and errors on the existing index.

Then: `git add -A && git commit -m "..." && git push origin dev`.

---

## What landed today

| ID | What | State |
|----|------|-------|
| **A177** | Sync deadlock: no fetch timeout + `_isSyncing` cleared only in `finally` → one hung request wedged ALL syncing (incl. Force sync) forever. Fix: `syncFetch()` AbortController timeout on all 15 fetches + `_syncStartedAt` stale-guard + break-batch-on-timeout. | **shipped, live** |
| **A178** | Sync was invisible + coupled. `runPushStages()` decouples the 5 push stages (a throw in one no longer skips order push); shift/price push failures + successes now hit the durable log; `getSyncStatus` gains `pendingBreakdown`; Tech menu gains **Test connection** (real reachability) + **View log** + the breakdown. | **shipped, live** |
| **A179** | Till expenses never synced: id was `exp_<ts>_<rand>`, not a UUID → 22P02 → 500'd the whole cash batch. Fix: `uuid()` generator + startup self-heal for stuck non-UUID pending expense ids. | **shipped, live** |
| **A180** | `/api/sync/push` 500'd the whole batch on one bad row (expenses were a batch upsert). Now per-row like shifts/floats/days; bad id → `rejected`, good rows land. `partitionByValidId`. | **shipped, live** |
| **A181** | **THE root cause.** Order numbers are `terminal_code--localSeq`; cloud was `UNIQUE(business,branch,order_number)`. A reinstalled/2nd till reused `T1--N` over the old till's numbers → server 409 → **client wrongly marked 409 as `synced`** → sales silently absent from cloud. **Part 1 (shipped):** 409 now surfaces as `failed`+logged, never synced. **Recovery (ran, test):** `scripts/recover-lost-orders.mjs` re-numbered the 7 stuck orders T1→T2 and re-queued (KES 12,510). | **pt1 shipped; recovery ran** |
| **A182** | MAC binding so a reinstalled till keeps its name/code (attacks A181's root). Desktop reads a stable MAC → `X-Device-Mac`/`mac_address`; migration 93 adds `user_devices.mac_address`; `findPriorTerminalByMac` returns the prior identity on enrol; till adopts it. + `docs/RESTORE-GUIDE.md`. | **built + deployed; migration 93 live** |
| **A183** | **The durable fix.** Migration 94: order-number uniqueness is now **per-device** `(business, branch, COALESCE(device_id,''), order_number)`. Two tills reusing `T1--N` coexist by device; genuine re-push still dedupes by idempotency_key; NULL-device web/legacy orders stay branch-unique. No code change needed. | **migration 94 LIVE; repo file pending (see above)** |
| **A184** | (Opened, not built) Terminals/fleet screen shows every till as "SwiftPOS till" — needs device name, terminal code, MAC, active cashier/shift, role, branch, and a retire/merge action for dead/duplicate terminals. | **open, register entry only** |

Tests added (all green on bench, better-sqlite3 real driver): `sync-timeout` 5,
`sync-decouple` 6, `expense-id-repair` 5, `sync-push-partition` 8,
`order-409-not-synced` 5, `machine-fingerprint` 8, `device-mac-restore` 7,
`order-number-per-device` 6. All wired into `test:desktop` / root `tests/`.

Verification strength (rule 9): desktop sync logic ran in its **real compiled form**
against a real SQLite driver (strong green) — this is how the deadlock and the 409
bug were both reproduced before fixing. Server pieces are `tsc`-clean + pure-logic
tested; the enrol-UI pre-fill and real-Windows MAC behaviour are **target-only**.

---

## Delivery artifacts (all on base `d5bd396`, applied incrementally)

`-m` A181 pt1 · `-n` recovery tool · `-o` A182 (server+desktop+migration 93) ·
`-p` A183 (migration 94 + test) — **`-p` did not apply on the user's box; migration
94 is live in prod but its repo file must be recreated by hand (above).**
Cloud SQL convenience: `docs/APPLY-MIGRATIONS-93-94.sql` (both migrations, one
transaction, with a sanity check) — already effectively applied via the runner.

---

## Pending — yours to run, in priority order

1. **Rotate the database password.** The user pasted a **live prod `DATABASE_URL`**
   (with password) into the terminal this session, and a GitHub PAT earlier in the
   saga. Rotate both. This is the only real security exposure open.
2. **Finish the repo commit** — recreate `migrations/94_...sql`, confirm
   `migrate.mjs --plan` = pending 0, commit + push (see UNFINISHED).
3. **Confirm `T2--6` / `T2--7` reached the cloud.** They were possibly still pending
   when the user deleted till `4396d282`. Check:
   `psql "$DATABASE_URL" -Atc "SELECT order_number,total FROM orders WHERE order_number LIKE 'T2--%' ORDER BY order_number;"`
   Expect `T2--1..T2--7`. If it stops at 5, look for a `swiftpos.db.bak-*` in
   `C:/Users/USER/AppData/Roaming/SwiftPOS/` to recover them (test env — low stakes).
4. **Prove the loop live:** ring one test sale on the new till (set code **T3**) →
   Force sync → confirm a fresh `T3--1` lands on the cloud Orders page. This is the
   one end-to-end check never yet run on live infra.
5. **A184** — the fleet-identity UI (today's request). MAC populates only after each
   till checks in on the A182 build.

## Standing backlog (older, non-urgent)

- Verify A167/A168/A169 attribution on a real offline sale.
- **Do NOT run `scripts/setup-clean-db.sh`** — it `DROP SCHEMA public CASCADE` =
  total prod data loss. `verify-db-schema.mjs` FAILs on 6 columns in
  `fuel_tanks`/`parking_sessions`/`category_stations` (verticals B Fastfoods doesn't
  use) + a `branch_settings.id` nullability note — all **pre-existing drift**,
  non-blocking. The `user_devices.mac_address` "extra column" is A182, correct;
  regenerate `scripts/schema-index.json` to clear that line.
- Duplicate migration **90** (`90_order_type_delivery_check` ran; `90_restore_order_types`
  never will — dedup by number) → `order_type` 'aggregator'/'other' still rejected on
  cloud. Duplicate migration 01 also exists.
- **Wire the desktop test suite into CI.** Three Windows-path slips this session
  (`import.meta.url` → use `fileURLToPath`; `%APPDATA%` vs `$APPDATA`; running from
  `apps/desktop` vs repo root) would all have been caught by CI on a real runner.
  This is the durable fix for that whole class.

## User context

Pragmatic, no-blame ("stop blaming the till, it's a piece of code that does what
it's told"), pushed hard against "it's the network" — which is what forced the real
deadlock discovery. Applies patches promptly on Windows (MINGW64, `/c/swiftpos/pos`,
patches in `/c/swiftpos/sp-patches/`), reports terminal output back. Tends to reach
for **delete-and-reinstall** when a till won't sync — this is what SPAWNS the
duplicate device_ids (three now: `f77f63d7`, `4396d282`, `55e8dd9f` for one shop).
A182 + A184 exist to make that unnecessary; gently steer away from reinstalling.
This is described as a **test environment**, so late-saga data loss is low-stakes.
