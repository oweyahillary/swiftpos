# MANIFEST 2026-09-10-D18 — D18 guarded (A186 attempt reverted)

**Base:** `origin/dev` @ `aa8011d`. **One new test + register.** No app-code change.

## D18 — tech token paste (FIX BUILT, guarded)
The fix already lived in `PinPage.tsx`: the reveal field's `onPaste` detects an `st2.` token,
`preventDefault()`s (bypassing the `maxLength={12}` + upper-casing that truncated it), sets the
FULL token, and jumps to the token step. This delivery adds the guard that locks it:
`tests/tech-token-paste.test.mjs` (6 checks, mutation-checked). Needs an on-screen paste confirm
on the amber build to move to CLOSED.

## A186 — NOT shipped (attempt reverted; recorded as OPEN with new evidence)
An earlier attempt added `await db.close()` before exit in 6 migration tests, on the theory it
would stop libuv's Windows async-close crash. **Verified on the owner's Windows box: it did NOT
work** — `test-migration-47` crashed *even with the close added*. So the crash is independent of
closing the handle. The 6 edits were reverted (a non-fix that looks like a fix is worse than an
honest open item). A186 stays **OPEN**, now with the exact failing files (47, 79, 41-42) recorded
and the `db.close()` cause ruled out. Next candidates (unverified, each needs a Windows run):
upgrade `@electric-sql/pglite`, or subprocess-isolate each migration test. Still P3 — Ubuntu CI is
the authoritative green; this is a local-Windows cosmetic annoyance.

## Files
| File | Change | ID |
|---|---|---|
| `tests/tech-token-paste.test.mjs` | NEW — D18 paste-routing guard (mutation-checked) | D18 |
| `docs/AUDIT-REGISTER.md` | D18 → FIX BUILT; A186 stays OPEN + new evidence; changelog | — |
| `docs/MANIFEST-2026-09-10-D18.md` | this record | — |

## What ran (rule 7)
```
tests/tech-token-paste.test.mjs            6/6 ; mutation-checked
offline suites  tests/*.test.mjs           97/97 (98 in run-all incl. server-built suites)
gates  register-consistency / doc-refs / root-clean / test-registration   OK
```

## NOT verified here (rule 16)
- D18: the on-screen token paste on the amber build.
- A186: still open — see above.

## Apply
```
git pull origin dev
# extract this zip over the repo root, then:
node tests/tech-token-paste.test.mjs        # 6/6
node scripts/check-register-consistency.mjs && node scripts/check-doc-refs.mjs
git add tests/tech-token-paste.test.mjs docs/AUDIT-REGISTER.md docs/MANIFEST-2026-09-10-D18.md
git commit -m "D18 guard (tech token paste FIX BUILT); A186 stays open — db.close() ruled out on Windows"
git push origin dev
```
Rollback: revert this commit — test + register only.
