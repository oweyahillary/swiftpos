# HANDOFF — 2026-08-14

Working rules unchanged — the §0 in `HANDOFF-2026-08-08-evening.md` (rules 1–23)
still governs. This session followed them; where a rule bit, it's noted inline.

---

## 0. The one thing to get right first: git vs delivered

- **In git (`origin/dev`, tag `v0.5.31`, commits `3f23c0f` + `a9d7be6`):**
  the dev/prod split (A68, D17) and enrolment-moved-to-admin (A69). Deployed and,
  for D17, verified on your screen.
- **Delivered but NOT in git** — everything after the push (A70, A71, A72, A73,
  D18, and the A12 fix). It's all in **one cumulative zip this session**:
  `swiftpos-2026-08-14-session.zip`. Base commit **`a9d7be6`**. Apply it over `dev`,
  `git status` first, then commit/push. `package.json` is deliberately excluded
  (rule 22) — your `0.5.31` stands until the desktop rebuild below bumps it.

Do not hunt for the seven earlier delta zips (f–l); this one supersedes them.

---

## 1. What shipped this session

| ID | What | Surface | Verified |
|---|---|---|---|
| A68 | Dev/prod favicon + title badge, env-driven | dashboard, admin | in git; set `VITE_APP_ENV` per Vercel project |
| D17 | Desktop dev/prod flavour (icon, appId, `%APPDATA%`, runtime cloud-host title) | desktop | in git; **seen working** — ready to close |
| A69 | Enrolment issuance → admin (branch-bound, licence-gated); owner `/enrol/code` retired to 410; **batch codes** | server, admin | in git (base); batch is in the session zip |
| A70 | Enrolled-device roster in admin | server, admin | session zip |
| A71 | Owner device detail (branch, role, last-active, version, enrolled) | server, dashboard | session zip |
| A72 | Devices owner-nameable + "not synced >1d" stale badge | server, dashboard | session zip |
| A73 | Fleet-health "Terminals" page made reachable (nav-drift fix) | dashboard | session zip |
| D18 | Tech token pasted into reveal field was truncated → `onPaste` routes it; admin also shows the reveal code | desktop, admin | session zip; **needs desktop rebuild** |
| A12 | Recipe drawer read a dead column → now reads live per-branch stock | server | session zip |

Also written (design only, no code): **`PHASE5-READPATH-PLAN.md`** and
**`DESKTOP-AUTOUPDATE-PLAN.md`** — both in the zip.

---

## 2. Deploy order (once the zip is applied + pushed)

Server first, then web, then desktop — same rule as always (routes must exist
before UIs call them; tills need the new build for their bits):

1. **Cloud / API (Render)** — A69 batch, A70, A71, A72 rename endpoint, A12.
2. **Dashboard (Vercel)** — A71/A72 device detail, A73 nav link. Set `VITE_APP_ENV=dev|prod`.
3. **Admin (Vercel)** — A70 roster, A69 batch prompt, D18 reveal code. Set `VITE_APP_ENV`.
4. **Desktop rebuild** — `npm run release:both` for D18 (paste fix). This bumps the
   version (rule 15); tag after the build, read the artifact filename first.

---

## 3. Live checks that close the open items (rule 16 — none provable on the bench)

- **A69** — ask for N codes → N distinct, each enrols one till then dies; old
  `/api/enrol/code` returns 410.
- **A70** — an enrolled till shows in the admin roster with right branch/role/version.
- **A71** — owner device row shows branch + real last-active time.
- **A72** — rename a device; it sticks after the till signs in again; another
  business's device can't be renamed; stale badge shows on a quiet till.
- **A73** — "Terminals" appears under Setup and opens the fleet table.
- **D18** — paste a real token at the reveal prompt → unlocked tech session.
- **A12** — Recipes drawer shows true per-branch stock, matching the Ingredients page.
- **D17** — already seen; mark closed when you're satisfied.

---

## 4. Blocked on you (nothing I can do from the bench)

- **A4** — your `swiftdb.sql` proved the repo reproduces **every table and column**
  in prod. The lone prod-only table `schema_migration_runs` is the runner's own
  (`scripts/migrate.mjs:96`), not a missing migration. So 68/72 touched only things
  a table-only export can't show (function / index / RLS policy / data / or a
  superseded no-op). To finish: run
  `select version, notes, applied_at from public.schema_migrations where version in ('62','64','65','66','68','72');`
  or a real `pg_dump --schema-only`. Send either and it closes.
- **A54** — mail undelivered; needs Resend or a Render plan change (SMTP port filtered).
- **Prod chores** — reset the `super_admin` password (never printed); rotate the
  Supabase DB password (shown partially once).
- **eTIMS** — the KRA certification decision (A vs B), table-stakes for VAT clients.

---

## 5. Decisions waiting, before the big work starts

- **Phase-5 read-path** (A17 + A18/19/20/24 — the "offline forever" cluster): plan
  is written (`PHASE5-READPATH-PLAN.md`). Needs **one owner decision** — does the
  staff roster replicate to every till (failover works, but a stolen peer yields
  PIN hashes)? Recommendation: split it (cashier PINs on, override PINs
  flaggable-on). Then I design the read-side cursor model (the one hard part).
- **Auto-update** (D3, unblocks A1 and de-risks every desktop rollout including
  Phase-5): plan written (`DESKTOP-AUTOUPDATE-PLAN.md`). Two decisions —
  buy an **OV code-signing cert** (the only hard external dependency), and flip
  `perMachine: false` (silent update can't elevate). Everything else is buildable
  now, unsigned, on a test machine.

---

## 6. Register movement this session

Opened: A68, A69, A70, A71, A72, A73 (A-side); D17, D18 (D-side). Fixed (pending
live check): A12. Narrowed with evidence: A4. Header re-derived, gate green,
register-consistency mutation-checked earlier in the session.

Current open counts: **A: 1 P0 · 9 P1 · 6 P2 · 5 P3 — D: 1 P0 · 2 P1 · 2 P2 · 3 P3.**

---

## 7. What I'd do next session, in order

1. **Apply + push the session zip, then deploy (§2) and run the §3 live checks.**
   Clears eight items from "code done, unverified" to closed.
2. **Close A4** with the one query in §4 — 30 seconds, and a P1 either closes or
   shrinks to one small migration.
3. **D4 → D1** — one two-business enrolment test kills a P0.
4. Then the fork: **D3 (auto-update)** for leverage, or **Phase-5** for the
   headline gap. D3 first is my steer — it makes every later desktop rollout,
   Phase-5 included, stop being a per-till site visit.
