# HANDOFF — 2026-09-24 (full day; owner present throughout)

**Read first:** `docs/WORKING-METHOD.md` (it gained four rules today — §9), then the rules in `HANDOFF-2026-08-08-evening.md` §0.
Standing rulings unchanged: every delivery is a zip + manifest (docs-only too); rule 21 covers UI strings; SwiftPOS is
general-purpose — no reference business named anywhere (now enforced by `scripts/check-reference-names.mjs`).

## Headline
- **A322 closed** — no reference business in shipped product; desktop **v0.6.4**.
- **Branding Phase 2 (A323) — four of five slices built and released**: registry (A324 ✔), `theme_id` + `themes` flag cloud→till
  (A325 ✔, v0.6.5), the till's colours follow the theme (A326, v0.6.6), the web theme picker + live preview (A327).
- Open: **0 P0 · 18 P1 · 17 P2 · 19 P3**. Tip `68f65c9`, CI #397 green. Desktop **v0.6.6** released and running on mamangina.

## Deliveries today (all landed, CI green unless noted)
| Delivery | Commit | CI | What |
|---|---|---|---|
| -a | `5b3ce9c` | #389 · Release #20 | A322: test print shows the till's own name; neutral sample data; checklists renamed, reference spreadsheets removed; name gate. **v0.6.4** |
| -b | `d69244a` | #390 | A322 closed (owner: preview "B Foods", placeholder, template) |
| -c | `d49cefb` | #391 | Phase 2 proposal revised: **two layers** (brand colour + curated action theme), seven themes; Render dev/prod clarified |
| -d | `d8428bd` | #392 | Slice 1 — `themes.ts` registry (A324); tracker A323 |
| -e | `b69eeca` | #393 · Release #21 | Slice 2 — migration 106 `theme_id`, `themes` flag, cloud→till, admin toggle (A325). **v0.6.5** |
| -f | — | — | never landed: test spawned `.bin/tailwindcss` (a `.cmd` on Windows) |
| -g | `8f40e02` | **#394 FAILED** | Slice 3 — the till's 222 greens classified (150 action · 4 brand · 68 stay), applied by script (A326). Red on a stale pin in A321's desktop test |
| -h | `bb31313` | #395 · Release #22 | CI fix (re-pin to intent). **v0.6.6** tagged here |
| -i | `26f88a3` | #396 | Slice 4 — web theme picker + till preview + save (A327); A326 target evidence recorded (closed) |
| -j | — | — | never landed: Node crashed on exit on Windows after the test passed (libuv) |
| -k | `68f65c9` | #397 | A327 fix: selected theme invisible in DARK mode → theme-coloured ring + ✓; test uses `process.exitCode` |

## State
- **Deployed:** migration 106 applied (dev DB); cloud (dev Render) from ≥ `b69eeca`; admin portal; dashboard — **deploy `68f65c9`
  (the -k fix) if not yet done**. Desktop v0.6.6 on mamangina.
- **B Foods:** themes ON; theme **Violet**; brand colour **#e6d300** (yellow) — set by the owner at the end of the day.
- Render: `swiftpos-20c2` is the **development** service (so `/health` "env: development" is correct); production is separate.

## Next session — in this order
1. **Close A327 (and A326's untested path) from one till check.** With B Foods on Violet + yellow brand, on the till after sign-in:
   Manager screen → a thin **yellow strip** across the top, **yellow-tinted sidebar**, **Violet** buttons/selections, **green prices**;
   optionally **Lock till** → curtain dots + Enter yellow. The owner's last screenshots (Branding page; PIN screen yellow) already prove
   web → cloud → till; this proves the Phase 2 pieces on the till. Record in a VERIFY-LOG + close A327.
2. **Slice 4b — the dashboard's own colours** (221 green uses in 61 files): same classify-first method as A326 (extract with context,
   rule-classify, hand-review, audit the status→action direction, owner approves, apply by script, ratchet gate). The dashboard is
   **dark-first** (light mode = overrides in `index.css`) — verify in BOTH modes.
3. **Slice 5** — the real-till walk of every theme (Teal and Blossom need the shop-lighting check), then close A323.
4. Per-branch overrides (after slice 5).

## Follow-ups logged today
- A320 siblings (branch/staff spaces-only names) · A321 point 4 (`branch_prices` trigger) · A318's 11 sibling tables.
- The node bundle relays no branding/theme (pre-existing; tills behind a node keep their local value) — A325 note.
- 56 tests still end with `process.exit(...)` (Windows exit race) — convert when touched (WORKING-METHOD §9).
- `schema-index.json` was merged from migrations for 106 — re-run `build-schema-index.mjs --from-db` when convenient.
- The web Branding page's existing labels (`text-gray-700`) are dim in dark mode — pre-existing; fix with 4b.

## Lessons added to WORKING-METHOD §9 today
Never spawn `node_modules/.bin/*` (a `.cmd` on Windows) · run EVERY test CI runs (run-all AND `apps/desktop/test`) and sweep for tests
pinning any edited line · check dashboard UI in dark AND light mode · new tests end with `process.exitCode`.

## Verify after
```bash
git fetch && git reset --hard origin/dev
node scripts/check-register-consistency.mjs && node scripts/check-doc-refs.mjs && node scripts/check-root-clean.mjs
```
