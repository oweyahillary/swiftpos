# HANDOFF — 2026-08-22

Working rules: `HANDOFF-2026-08-08-evening.md` §0, rules 1-23. **No new rules.**
This is a *catch-up* handoff: the last one (`HANDOFF-2026-08-17.md`) was written at
the A108 mark, and 28 commits (A112→A139) then shipped to `dev` over 08-17 → 08-21
with only per-batch manifests behind them. This reconstructs that arc from the
commit log, the manifests, and the register changelog so the next session is not
reading 28 commits cold.

The register (`AUDIT-REGISTER.md`) wins where this disagrees with it. Next free
general ID is **A140**.

---

## 0. TOMORROW, BEFORE ANYTHING ELSE

**1. `dev` and `main` have diverged — production is a full week behind.** `main`
tips at `9906e0b` (the A111 merge, 2026-08-17 14:57); `dev` tips at `6c7495b`
(A139, 2026-08-21 22:39). That is **28 commits, A112→A139, that have never been
promoted to `main`.** The 08-17 handoff opened with "dev and main byte-identical,
the cleanest a handoff has started" — that is no longer true. The cloud API
auto-deploys from `main`, so **none of A112→A139 is in production** unless it was
promoted out of band.

**2. Two of the unpromoted items are silent data-loss fixes, and each needs a
prod migrate that has not run.** Treat these as the priority:
- **A128** — custom-method / room_charge sales silently never synced to the cloud
  (`payments.method` was value-checked + `varchar(20)`). Fix = **migration 89**.
- **A129** — delivery sales silently never synced (`orders.order_type` dropped
  `delivery` in migration 58 while the till kept accepting it). Fix = **migration 90**.
  A129's code fix is on `dev`, not `main`.

  If prod is still on `main`/A111, custom-tender and delivery sales are **still
  failing to reach the cloud right now**. Confirm what is actually deployed before
  anything else.

**3. Three migrations are pending prod (86 → 91): 89 (A128), 90 (A129), 91
(A139).** Prod is migrated **only** by the `DB migrate (production)` Action, never
by hand. Migrate first, then promote the cloud API — the A128/A129 server code
expects the widened domains.

**4. Migration numbering has a collision to clean up.** There are two files
numbered **90** — `90_order_type_delivery_check.sql` and
`90_restore_order_types.sql`, both the A129 fix. The runner keys applied
migrations by full filename, so both apply and nothing is silently skipped, but
one is redundant and the "run in order by number" model is broken. Decide which
is authoritative (the tested one is `90_order_type_delivery_check`) and retire the
other, renumbering to 92 if it is still wanted.

---

## 1. What this handoff covers

The A112→A139 arc, grouped by theme, then the 2026-08-22 session (register
trueing-up, migration 68 recovery, schema-index regen). It does **not** re-open
anything the register already tracks; for per-item status the register wins.

## 2. Current state

- **Branches:** `dev` @ `6c7495b` (A139). `main` @ `9906e0b` (A111). See §0.1.
- **Gates:** all green on the bench as of 2026-08-22, **including
  `check-schema-drift`**, which was red (stale `schema-index.json` missing
  `branch_settings` from migration 91) and was regenerated this session — see §4.
  Caveat: the regen used `--merge-migrations` (best-effort, no DB on the bench);
  a `--from-db` re-run should follow when the cloud DB is reachable.
- **Bench limits (rule 9):** Linux, Node v22.22.2. Nothing desktop/SQLite/Electron
  can run here; every green is static/type-level. Target remains Windows / Node 20
  / Electron 35 for the till.
- **Pending prod migrates:** 89, 90, 91 (see §0.3).

## 3. What shipped A112→A139

**Tech-access hardening (A112–A115).** A112 reconciled the register header to the
tree. A113 retired the v1 HMAC tokens and the hardcoded default secret. A114 made
the tech reveal code stable-per-branch, auto-provisioned and self-healing on sync,
with a backfill migration. A115 added a health-monitoring runbook and a direct
Supabase keep-alive workflow.

**Admin-portal build-out (A117–A127).** The largest theme. Plan + glass mockup
(A117); revoke till, rotate reveal code, fix health chart (A118); edit business +
change owner email (A119); create branch, admin-only (A120); branch lifecycle +
suspend/purge foundation and Stage 1 (A121–A124); purge Stage 2 non-destructive
dry-run preview (A125); Phase-3 glass refresh (A126); Branches tab with tills +
tech-audit drill-down (A127).

**Silent-sync data fixes (A128, A129).** See §0.2. Both are cloud-domain
regressions where the till accepted a value the cloud rejected, dropping the sale
silently. A129 also added the `check-push-domain-parity` gate so this class of
drift fails CI in future. **A130** was opened alongside: the Aggregators report
queries `order_type='aggregator'`, which no path writes — a dead report awaiting a
wiring decision, not a widen. (Still OPEN.)

**Dashboard/KDS (A131–A135).** Delivery orders now deduct packaging uniformly
with takeaway (A131). Dashboard nav UI: accordion + desktop SVG icons, labels
unchanged (A132). Owner Settings consolidated into three tabbed sections — Users
& access / Devices & printers / Business — with back-compat redirects (A133).
A134 added the Business › Profile tab (owner-editable identity + business-wide
receipt header/footer + 24h; currency locks after first sale). A135 surfaced and
fixed two pre-existing runtime bugs that the A133 reshuffle made reachable — KDS
board rendering blank and add-table failing silently — plus a nav active-state fix
and a dashboard-wide error-surfacing sweep.

**API drift gates (A136).** Added `check-api-routes.mjs` and
`check-api-schema-drift.mjs` to CI with self-tests, and fixed the drift they
caught (e.g. `stock_movements` products-join, staff `/clock` pin_hash verify).

**Tables + error-surfacing (A137, A138).** Bulk-create tables ("Add multiple",
T-numbered) (A137). Catch-less mutation sweep so Parking/Minimart/Petrol settings
surface save/delete errors like Restaurant does — zero swallowers remain (A138).

**Franchise per-branch overrides (A139).** Per-branch receipt text and opening
hours that override the business-wide defaults, resolved server-side. Cross-stack,
including the desktop till. Adds **migration 91** (`branch_settings`). Pending
owner sign-off.

**Runtime thread (A108/A111, carried).** Node runtime standardisation — A108
moved 20→22 and swept npm vulns to zero; A111 opened "standardise on Node 24 LTS"
(root `package.json` now wants Node ≥24). The desktop Electron bump stays blocked
on the two-till build per rule 9.

## 4. This session — 2026-08-22

Docs + one recovered file; no behavioural code changed. See
`MANIFEST-2026-08-22-a.md`.

- **Code↔register audit.** Went through the open list against the source. Several
  fixes had shipped (mostly the 08-14 batch) but their register headings never
  flipped — the register trailing the code.
- **Closed A68, A71, A72** (deterministic wiring, verifiable from source):
  env deploy badge wired in both web apps; `DevicesTab.tsx` device enrichment +
  rename + stale badge.
- **Annotated A69, A70, D18 — kept OPEN.** Code-complete on `dev`, but they need a
  browser pass before close (rule 16). Same for A73 (register says its nav link
  was restored; not re-confirmed on bench) and A12 (fix applied, pending live check).
- **Recovered migration 68 → closed A4.** The lost `increment_loyalty_points`
  param-rename file (`p_delta`→`p_points`) is back in `migrations/`. Live signature
  confirmed by the owner as `p_customer_id uuid, p_points integer`, matching
  migration 53, `functions-index.json`, and `orders.ts` on both branches. No prod
  run: the ledger row was pre-seeded in `schema_migrations`, so the Action skips
  it; the file is idempotent if a fresh DB ever runs it.
- **Regenerated `schema-index.json`** via `build-schema-index.mjs --merge-migrations`
  (added `branch_settings` + 14 columns, removed nothing). `check-schema-drift`
  now green. **Follow-up:** re-run `--from-db` against the live cloud DB when
  reachable, per the tool's own guidance.

## 5. Open items

The register is the source of truth; open count after this session is
**A: 1 P0 · 9 P1 · 8 P2 · 3 P3 — D: 1 P0 · 2 P1 · 2 P2 · 3 P3**. Highlights:
- **A17 (P0)** — node-configured till no longer time-expires (fix code-live);
  awaiting the two-till hardware test.
- **D1 (P0)** — owner with two businesses dead-ends at desktop login.
- **A129 (P1)** — code on `dev`, needs prod migrate + promotion (§0).
- **A19 / A20 / A24 (P1)** — offline-peer sync gaps; the node is a replica, not a
  relay, so a permanently-offline till's sales never reach the cloud, the roster
  does not replicate for failover, and reference data goes stale. Unbuilt.
- **A54 / A50 (P1)** — mailer supports Resend; blocked on the owner (key + domain).
- **Confirm-in-browser, then close:** A69, A70, D18, A73.

## 6. Next steps

**Immediate (unblocks production):**
1. Confirm what is actually deployed to the cloud (branch + commit).
2. Run `DB migrate (production)` to take prod 86 → 91 (migrations 89, 90, 91).
3. Promote `dev` → `main` (PR), so A112→A139 — including the A128/A129 sync fixes
   — reach production. Migrate before the API deploy.

**Then (housekeeping, nobody blocked):**
4. Retire the redundant migration-90 file (§0.4).
5. Re-run `build-schema-index.mjs --from-db` against the live DB (§4 caveat).
6. Browser-confirm A69/A70/D18/A73 and close.
7. Owner sign-off on A139 (franchise overrides) and A133/A134 (settings).

## 7. Gotchas — read before re-deriving

- **Migration 68 is not a pending prod change.** The live function already matches
  (`p_points`); the file is in the repo purely for fresh-DB parity, and the ledger
  row is pre-seeded so the Action will not touch the live function. Do not "apply"
  it.
- **`schema-index.json` was regenerated best-effort, not from the DB.** It is
  green but should be re-derived `--from-db` before you trust it as ground truth
  for a new schema-audit finding.
- **The migrate runner keys on full filename, not number.** That is why the two
  90-files both apply; it is also why renumbering the redundant one to 92 is safe.

## 8. The pattern, restated

The recurring shape this arc surfaced twice (A128, A129) is the same one the
push-domain-parity gate now guards: the till accepts a value the cloud silently
rejects, and the sale vanishes with no error. Rule 7 (never "should work" — say
what ran and what it printed) and the new gates exist to make that class loud.
The second shape is this handoff's reason for existing: **docs trailing code.**
Keep the register heading and the changelog moving with the commit, not a week
behind it.
