# HANDOFF — 2026-09-22 (lead dev session; owner present)

**Rules:** unchanged — `HANDOFF-2026-08-08-evening.md` §0, rules 1–24. One correction block added above
the §0 environment note (fleet is Electron 43.4.0). Owner ruling today: **rule 1 governs every delivery,
docs-only included** — the rule-18 "no archive" exception is retired. Rule 21: **UI strings are in scope.**

## What happened, in order (`dev` `2015906` → `9869c9d`, nine commits)
| Delivery | Register | Commit | What |
|---|---|---|---|
| -a | A309 | `4b90c87` | CI red on `dev` since #360 (`check-doc-refs` on an abbreviated filename list in the -q manifest). Line fixed, gate untouched. |
| -b | A309, A295, A237, §0 | `23eeefa` | A309 closed; **A295 opened as the Phase 1 branding tracker**; A237 re-graded (Go bridge live, Node bridge retired); scope-doc status corrected; `VERIFY-BRANDING-PHASE1.md`. |
| -c | — | `e84b744` | The -a manifest, which my -a push block had omitted (my error; doc-refs went red again for one commit). |
| -d | A306, A159 | `b4ec4fd` | Main-side `isManager()` gate on `update:installNow`; `TERMINAL_WRITE_ENFORCE` in `.env.example` + `render.yaml`. |
| -e | A310, A314 | `1286f0e` | **Receipt logo 1/4:** `shared/printing` mono raster + `image` block + `GS v 0`; receipts only. A314 opened (stale SAMPLE-OUTPUT/out bins). |
| -f | A311 | `1e26459` | **2/4:** migration 105 `receipt_logo_enabled` (opt-in), local schema **53**, `/pos/init` + pull carry raster + toggle, server CRUD validates the raster shape. |
| -g | A312 | `362c045` | **3/4:** till prints it when enabled; tech feed sends pixels, main thresholds, mono preview + toggle. Paper `.bin`s delivered. |
| -h | A313 | `9869c9d` | **4/4:** web Branding page toggle + receipt preview; web live/routed/reprint receipts carry the logo; bundle rebuilt + reproducible. |
| -i | A310→CLOSED, A315 | (this) | **Paper PASSED on XP-80.** A315 opened (duplicate closing line). VERIFY §F. This handoff. |

## Decisions made today (owner-ratified unless marked)
- Receipt logo stays in Phase 1 (lead dev; not overruled). It is a **client toggle, opt-in, default off** (owner).
- Toggle lives on the web Branding page; the tech feed has the same switch.
- Threshold once at upload (where a canvas exists), store packed bits; a till never decodes an image.
- Node print bridge retired on tree evidence; files removed in a non-deploy window (A237).

## Rule-24 catches (recorded so nobody trusts the pattern blindly)
Three times today a first-cut assertion passed for the wrong reason: -d (indexed a comment), -f (recomputed
the keep-flags instead of running the source's lines), -g (a mutation that printed nothing because the build
had silently broken). Each was caught by the mutation step and fixed before shipping. When writing
source-extraction tests: **extract the block and execute it**; never re-derive it in the test.

## Owed on target (in order)
1. ~~Paper~~ — DONE, XP-80, RAW spool, perfect.
2. **Migration 105 on prod** — additive, idempotent, safe live.
3. **Cloud deploy** (-d, -f touched `apps/server`; `render.yaml` pins `TERMINAL_WRITE_ENFORCE: "false"`).
4. **Dashboard deploy** (-h). Then VERIFY §F.1 in a browser.
5. **Desktop build 0.6.2, tag AFTER the build** (rule 15; -d, -f, -g stacked since 0.6.1).
6. **Till pass:** VERIFY A–D (A308, A278) + §F.2–F.5 (A311–A313 → A295). Also the A306 banner on the 0.6.2 update.
7. Unchanged from 09-21: A277 CTL receipt line; A19 relay pilot; A276 repro; A159 flag flip after the dry-run logs.

## Queue (bench, no owner input needed)
- A315 fix + A314 artefact refresh in one byte-affecting delivery.
- A237 Node-bridge deletion (non-deploy window) + `apps/print-server/README.md` rewrite.
- Rule 21 sweep (199 "the server" comments + UI strings) with a `check-vocabulary` gate.
- Stale-doc banners: RUNBOOK §2, TEST_TOOLING/TEST_PLAN, README "/kds no auth", BRANCH_AUTHORITY status.
- `run-all.mjs` narrower than CI (skips desktop main tsc, desktop suites, shared/printing).

## Register
0 P0 · 19 P1 · 17 P2 · 24 P3 (A-P3 unchanged by -i: A310 closed, A315 opened). Tree: migrations → 105
(105 NOT on prod), `LOCAL_SCHEMA_VERSION` 53, desktop package.json still 0.6.1 (bump at build).

## Verify after (as every handoff)
```bash
git fetch && git reset --hard origin/dev
node scripts/check-register-consistency.mjs && node scripts/check-doc-refs.mjs && node scripts/check-root-clean.mjs
```
