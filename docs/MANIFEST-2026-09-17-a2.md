# MANIFEST 2026-09-17-a2  (server surface — re-issue of -a; deploys via dev → Render)

Same server change as -a; re-cut because -a was superseded in the workspace and
never landed. **You must EXTRACT this over the tree** (git add alone did nothing
because pos.ts on disk was unchanged).

## Files
- `apps/server/src/routes/pos.ts` — A291: GET /api/pos/catalogue-version (read-only,
  fail-soft freshness endpoint the till already polls).
- `docs/MANIFEST-2026-09-17-a2.md` — this manifest.

## Apply (from repo root) — EXTRACT FIRST
    cd /c/swiftpos/pos
    # extract this zip over the tree so pos.ts is actually updated on disk, then:
    grep -c "catalogue-version" apps/server/src/routes/pos.ts   # must print 1
    git add apps/server/src/routes/pos.ts docs/MANIFEST-2026-09-17-a2.md
    git commit -m "A291: /api/pos/catalogue-version endpoint (server half)"
    git push origin dev   # Render redeploys

## Verify after Render redeploys
    curl -s -H "Authorization: Bearer <token>" \
      "https://swiftpos-20c2.onrender.com/api/pos/catalogue-version?branch_id=<branch>"
    # -> {"version":"2026-09-17T..Z"}
