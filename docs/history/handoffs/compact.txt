SwiftPOS Engineering Session — Compaction Summary
PROJECT
Multi-tenant Kenyan restaurant POS SaaS ("SwiftPOS"). Stack: Node/Express + TypeScript backend (Render); Supabase (Postgres/Auth/Realtime/Storage); React/Vite apps/dashboard (owner + POS) and apps/admin (AdminPortal.tsx, single ~1900-line file); Electron desktop (out of scope). Deploying to FIRST client ("Test Café Corner"). Repo C:\swiftpos\pos, branch security-hardening, Windows/MINGW64. Original code at /mnt/user-data/uploads/pos.zip.
WORKING PATHS (container resets between turns — ALWAYS re-establish)

Original extract: cd /home/claude && mkdir -p pos_audit && cd pos_audit && unzip -q /mnt/user-data/uploads/pos.zip
Cumulative patch tree: cd /home/claude/prev_patch && unzip -q /mnt/user-data/outputs/swiftpos-patch.zip (dir swiftpos-patch/)
PRIMARY DELIVERABLE: /mnt/user-data/outputs/swiftpos-patch.zip (71 files, cumulative). Apply by copying apps/ + migrations/ over repo root. PATCH_NOTES.md inside = round-by-round changelog.
Other outputs: SwiftPOS_Financial_Model.xlsx, SwiftPOS_Audit.md, handoff.md (just created — full goal/state/files/changes/failed-attempts/next-steps/test-checklist).

CRITICAL RECURRING ISSUE — STALE BUILDS
User repeatedly re-reported already-fixed bugs because they tested stale builds (Vite HMR misses overwrites on Windows). After patch: copy WHOLE apps/ tree + restart each dev server + hard-refresh + restart API. Self-check: grep -c "<fixstring>" <file> before assuming a code bug. Multiple "still broken" reports (incl. Post-to-Room, checkout, admin silent-zero) were stale builds.
BUILD/VERIFY WORKFLOW
Diagnose root cause from code → edit in prev_patch tree (sendError-patched files live there; edit in place) OR pos_audit then copy → build-verify (cd apps/dashboard && npm run build exit 0; cd apps/admin && npm run build; server npx tsc --noEmit --skipLibCheck, baseline ~130 pre-existing errors that build transpiles past) → append PATCH_NOTES.md → re-zip to outputs → present_files. Some files sendError-patched only in prev_patch (orders.ts=12 sendErrors, shifts.ts, admin.ts, schemas.ts, inventory.ts, loyalty.ts) — edit those in bundle directly; others copy from pos_audit.
COMPLETED — SECURITY AUDIT (all bundled)
C2 M-Pesa callback IP allow-list+validation+idempotency; H1 KDS JWT (BROKE frontend KDS — still PENDING, KDSPage sends no token → 401); H2/H3 secrets throw + webhook SSRF; M1 deactivated-token; M2 JWT HS256 pin; M3 M-Pesa→DB (migration 21); M4 onboarding atomicity; M5 error leakage → lib/sendError.ts (generic msg + server-logged ref) across ~37 files; L1/L5 misc. PENDING user action: rotate C1 secrets (Supabase service-role, JWT_SECRET, ADMIN_JWT_SECRET, TECH_HMAC_SECRET, Resend/SMTP).
COMPLETED — QA FIXES (rounds 1-12, all bundled, build-verified)
Checkout (THE headline, root cause found round 10): apps/dashboard/src/pages/pos/cashier/useCart.ts confirmVariants stored variants as raw option {id,name,price_adjustment}, discarding group; backend recompute (orders.ts) + order-item insert key on {groupName,optionName} → "Unknown variant: undefined/undefined" 400 on 100% of variant orders. FIX: toggleVariantOption carries {...option,groupId,groupName}; confirmVariants maps canonical {groupId,groupName,optionId,optionName,priceAdjustment}. Backend orders.ts recompute ALSO accepts option id fallback (variantAdjById). USER CONFIRMED WORKING (African Breakfast KES250 sale, stock 15→14). Plus: idempotency_key in order body + synchronous chargingRef double-submit guard (PaymentModal.tsx); named missing-field 400 (Missing required fields: branch_id); empty-branch guard before charge.
Shifts: close-shift 500 → migration 22_shift_close_and_float.sql (adds shifts.expected_cash/cash_variance/closing_float/closed_at/notes IF NOT EXISTS + creates float_transactions table, cashier_id no strict FK) + CloseShiftSchema notes: z.string().nullable().optional(). USER CONFIRMED working (float in/out + reconciliation math correct).
Session-expiry (POS+Admin): POS posRequest (POSAuthContext.tsx) only handled PERMISSIONS_CHANGED 401 → rewrote to handle ANY 401 (refresh+retry, else set swiftpos_pos_expired flag + clear session → PIN lock); POSLoginScreen.tsx shows expiry msg. Admin AdminPortal.tsx req() ignored 401 → now clears token + swiftpos_admin_expired flag + returns to login; admin LoginPage shows msg. Admin fleet DashboardPage swallowed errors (.catch(console.error)) rendering misleading zeros → now shows error + Retry.
Reporting/dates (round 12): systemic new Date().toISOString().slice(0,10) = UTC date = yesterday for EAT (UTC+3). Created apps/dashboard/src/lib/localDate.ts (localDateStr), replaced across OverviewPage, ReportsPage, ReservationsPage, ZReportModal, POSReportsTab, ManagerReportsPage, ManagerDashboard, ExpensesPage. Backend reports.ts getDateRange mixed UTC/local parsing → now interprets filter dates as EAT business day (BIZ_TZ_OFFSET='+03:00', TODO per-business). NEEDS USER VERIFY (couldn't test without their data).
Stability/UX: App.tsx ErrorBoundary now wraps POS+Manager (was owner-only); ReceiptView.tsx null-business guard (if(!business)return); Print Bill freeze → printGuestCheck rewritten popup→hidden-iframe print w/ onafterprint cleanup (CashierScreen.tsx, same for room slip); Post-to-Room payload rebuilt to order API contract (was missing order_number+payments, flat product_id) — method 'other', room+guest in reference; table auto-clear via new onPaid prop (PaymentModal useEffect fires onPaid on completedOrder → CashierScreen deletes openOrders[activeKey]) NEEDS VERIFY; Transfer-table text fallback; localPrintServer.ts URL now VITE_PRINT_SERVER_URL (was hardcoded localhost:3001 = API port, caused /health 503 spam), disabled if unset; negative-price fraud vector → products.ts rejects negative base_price/cost_price + orders.ts recompute clamps unit=Math.max(0,...).
Earlier rounds (bundled): admin blank-screen/undefined-type/token-gen/native-confirm-freeze→useModal hook/tech-404/expiry-display/silent-401; POS cart "undefined:undefined" variant display, inventory adjust 500 (created_by: req.isOwner?null:req.userId), checkout crash→ErrorBoundary, staff PIN email dropped→added, Z-report spacing (.zreport-print scoped styles, was print-window-only), category truncation→break-words, café-naming.
NEW FEATURE — owner password reset: POST /api/admin/clients/:id/reset-owner-password (requireAdmin, validates ≥8, supabase.auth.admin.updateUserById(owner_id,{password}), audits without logging pw) + "Reset Password" button on admin client detail (askPrompt modal). Owners auth via Supabase Auth (businesses.owner_id=auth user id). SQL alt given: UPDATE auth.users SET encrypted_password=crypt('New',gen_salt('bf')),updated_at=now() WHERE email='...'.
PENDING / OPEN (carry forward)

VERIFY (just shipped, untested on real data): reporting timezone fix (Cockpit "Revenue Today" + Reports today should show sales), table auto-clear after payment.
User must: apply LATEST patch + rebuild BOTH frontends + restart API; run migrations 21+22 in Supabase SQL editor; rotate secrets; set env vars (ADMIN_JWT_SECRET, TECH_HMAC_SECRET [server won't boot without], MPESA_ALLOWED_IPS, MPESA_ENVIRONMENT=production, NODE_ENV=production, VITE_SUPPORT_WHATSAPP [need real #, falls back to placeholder 254700000000], optional VITE_PRINT_SERVER_URL).
KDS broken: KDSPage.tsx sends no auth token → 401 on hardened backend. Needs auth follow-up.
In-progress/untested (user set aside): eTIMS (KRA), M-Pesa, On Account.
Unbuilt Menu placeholders: Hold Orders, Create Orders, mid-sale Customers/Products/Payments/Discounts. Hold Orders + mid-sale Discounts are restaurant-expected.
Proposed/deferred: cart persistence (server-side draft model); inventory→POS stock enforcement (currently addable by design); auth consolidation (owner off Supabase → in-house; DO LAST, post-first-client, never mid-onboarding; bank now: new created_by-style cols nullable, NO hard FK to users).
Offered not built: force-password-change-on-reset flag; competitor pricing one-pager (Techlion/SimbaPOS/CuteProfit/EliteTeQ); 12-month cashflow tab; git .patch; copy+restart script.
Cosmetic: guest dialog light-mode in dark POS; POSDrawer CSS shorthand warning; mismatched seed images (African Breakfast/Ugali managu = book cover); category name no uniqueness constraint.

FAILED ATTEMPTS / MISDIAGNOSES (don't repeat)
Checkout 400: chased idempotency_key (Zod strips, not it) then branch_id before user's fetch-monkeypatch revealed variant shape → go to payload capture first for contract bugs. Shift-close 500: mis-attributed to closeErr path; "Internal server error"=uncaught throw=global handler, real gap=missing schema. Session-expiry: first fix listened for event POS client never emitted; real fix in posRequest. Double-submit: first guard exposed ReceiptView null-crash (no boundary). Post-to-Room + admin-silent-zero + checkout: re-reported "broken" were stale builds.
BUSINESS/PRICING (non-code, decided)
3 annual per-branch tiers: Starter 18k ("50/day"), Restaurant 36k (target, "100/day"), Pro 54k ("150/day") + 10k one-time onboarding; bill ANNUALLY UPFRONT via M-Pesa Paybill (no auto-debit); enforce via built-in renewal clock (web_access_expires_at). First client: 10k onboarding + ~24k Y1 discounted launch-partner. Hosting: Supabase Pro (~$25/mo) + always-on host (~$7) = ~$32/mo floor; NO free tiers (cold-start fails demo). Break-even ~2 clients. Financial model xlsx: 4-sheet editable P&L (6/20/50 clients net ~175k/520k/1.13M KES).
CURRENT STATUS
Checkout WORKS (user-confirmed). Handoff.md just delivered. Latest zip = 71 files. Awaiting user to apply+rebuild+run-migrations and verify the two freshly-shipped fixes (reporting timezone, table auto-clear). If reporting still 0 after: need order's created_at + exact query. If any new bug: named 400 errors + server-logged sendError refs now aid diagnosis.