# SwiftPOS — Session handoff, 2026-08-25

## How to deploy this session's work

Two equivalent artifacts, both = the same **48 files**, both apply on base commit **`189e597`**:

- `swiftpos-2026-08-25.patch` — verified `git apply --check` clean at `189e597`.
- `swiftpos-2026-08-25-m.zip` — cumulative, mirrors repo root (supersedes `-a`…`-l`).

```
cd /c/swiftpos/pos && git checkout dev && git pull --ff-only origin dev   # expect 189e597 base
git apply --check swiftpos-2026-08-25.patch && git apply swiftpos-2026-08-25.patch
cd apps/dashboard && npm install         # <-- REQUIRED: package.json gained "xlsx"; lockfile not shipped (rule 22)
cd ../.. && git add -A && node scripts/run-all.mjs
git commit -m "2026-08-25: A165 menu importer + A166 price tools (on node-authority/device-grant/A159)"
git push origin dev
```

Neither artifact ships a `package-lock.json` — that's why the `npm install` step matters. Expect the
known Windows PGlite `UV_HANDLE_CLOSING` migration flake as the only red in `run-all.mjs`; the
assertions themselves pass.

## What landed today (all on a green branch — server + dashboard `tsc`, all tests, all gates)

| ID | What | State |
|----|------|-------|
| A161 | A24 reference channel (node serves catalogue to offline peers) | built, **bench-only** |
| A162 | A19 sales relay (node forwards an offline peer's sale, once) | built, **bench-only** |
| A163 | A20 roster channel (node replicates staff for PIN failover) | built, **bench-only** |
| A164 | Cloud device-grant (Phase 1, server half) + migration 92 | built + real Postgres test; **inert** |
| A159 | Terminal write-guard enforce-readiness audit + shift-close allowlist fix | audited + fixed |
| A165 | Menu importer — sparse product upsert (1), choices/recipe/ingredients (2), unified "Menu upload" UI (3) | built; server real-tested, UI `tsc` |
| A166 | Bulk price tool (category + row-select) + inline price edit | built; math mutation-checked, UI `tsc` |

Verification strength (rule 9): server + migration ran in their real form on the bench (stronger
green); desktop and dashboard are `tsc`-clean but their runtime behaviour is target-only.

## Pending — target/prod, in the order to do them (all yours to run)

1. **Two-till + node hardware passes** to CLOSE A19 / A24 / A20 — use
   `swiftpos-node-authority-test-checklist.html`. Nothing offline is proven until this passes.
2. **Prod-migrate 91→92** (adds `user_devices.device_secret_hash`; additive, idempotent).
3. **A159 enforce:** confirm the dry-run logs show no `[terminal-write-guard] … would block` for a
   legitimate write, then set `TERMINAL_WRITE_ENFORCE=true`.
4. **A164 desktop cutover** (till stores its device secret + uses `/api/auth/device-token` on refresh
   failure) — only AFTER step 3, and hardware-verified (the till must sell/read as a non-owner).
5. **Versioned desktop build → tag AFTER the build** (rule 15), once A19/A24/A20 are closed.

## Open decisions (not blocking)

- **86-on-the-till:** a mid-service out-of-stock toggle belongs on the till/floor, not the dashboard
  (the dashboard already has back-office Enable/Disable). Needs a sync-design decision. Not built.
- **Importer preview** is client-side/structural; a server create-vs-update diff needs dry-run modes
  on the import endpoints.
- **Ingredients import** isn't sparse yet (requires `unit` + a branch, full-overwrite) — worth
  aligning with the other tabs.
- **Retire** the stock page's separate `BulkIngredientImport` now the unified uploader covers it.
- **Row-level scope** for the importer/price beyond category (partly done — price has row-select).

## Standing item (raised every session)

- **Rotate the GitHub PAT.** It was pasted in plaintext and pushed with repeatedly today.

## Bench facts for next time

- Server + dashboard build/test on the bench (Linux/Node 22): `cd apps/server && npm install
  --ignore-scripts && npx tsc`; dashboard likewise + `npx tsc --noEmit`. Desktop SQLite/Electron
  cannot run here — `tsc` + plain-node logic tests only.
- Base for all of today: `189e597`. Register IDs used up to **A166**.
