# HANDOFF — 2026-09-03 (cloud/dashboard + admin: verify & close-out)

Working rules are unchanged — see **HANDOFF-2026-08-08-evening.md §0** (the 24 standing
rules). The register (`docs/AUDIT-REGISTER.md`) is the source of truth and is fully up to
date as of this handoff. Continues **HANDOFF-2026-09-02.md** (which built the cloud/dashboard
batch); this session **verified it on prod, closed what passed, and cleared the admin section.**

> **⚠️ READ FIRST — "merged" is not "live" on this project.** It bit twice this session: prod
> was wired to serve `dev` not `main`, and then a **failed Render deploy** left prod on an old
> build. Both looked like code bugs; both were deploy. After ANY deploy, probe `/health` + one
> new route (401 = present, 404 = stale) before testing or building on top.

---

## 1. Goal
Verify the 2026-09-02 cloud/dashboard batch on **production**, close what passes (rule 16 —
live evidence only), and clear the **admin-portal** section. No new feature scope beyond the
one bug found while testing (A198).

## 2. Current state
- **`dev` = `7f121a4`** (register close of A196/A198). **`main` = `1812b3c`.** `dev` is **1
  commit ahead** — the A196/A198 register close, to be merged with this handoff in the next PR.
- **Counts:** A: **1 P0 · 17 P1 · 19 P2 · 6 P3** — D: 1 P0 · 2 P1 · 2 P2 · 3 P3. The P0 and
  most P1s are **parked desktop** items (§6); the active cloud backlog is small.
- **Deploy state (verified read-only):**
  - **Prod server `swiftpos-prod-mype`** — redeployed from `main`, **current**: `/health` 200
    `schema:ok`, `/api/admin/migrations` → 401 (route present). Migration 96 confirmed applied
    on the prod DB (a live void wrote `voided_by` with no 500). Migration 92 confirmed (the
    `schema-index` regen picked up `device_secret_hash`).
  - **Admin frontend `swiftpos-admin-eta.vercel.app`** — current (Migrations page + A198 card).
  - **Test box `swiftpos-20c2`** — serves `dev`, `env=development`, disposable data.
- **10 items closed this session, all on live evidence.** Admin section went **4 → 0**.

## 3. Active files (touched this session)
Server:
- `apps/server/src/routes/orders.ts` — **A196**: void deletes the order's kitchen ticket(s) by
  `order_id`; refund deletes only if `status='new'`. Best-effort + logged (mirrors the credit
  reversal), never throws — the void/refund already succeeded.
- `apps/server/src/routes/admin.ts` — **A154**: `GET /api/admin/migrations` (requireAdmin),
  reads `schema_migration_runs` + `schema_migrations`.

Admin:
- `apps/admin/src/AdminPortal.tsx` — **A154** MigrationsPage import + nav + page route (adapts
  its `req(path)` contract); **A198** renders the enrol-code card on the Branches tab.

Scripts / docs:
- `scripts/check-api-schema-drift.mjs` — **A154** `RUNTIME_TABLES` allowlist for
  `schema_migration_runs` (bootstrapped by `migrate.mjs`, not a numbered migration).
- `scripts/schema-index.json` — refreshed from the live DB for **migration 92**
  (`user_devices.device_secret_hash` / `device_secret_set_at`); fixed the PR #5 schema-drift gate.
- `docs/AUDIT-REGISTER.md` — the closes + new entries below.

## 4. Changes made (by ID)
- **A187 / A191 / A3 / A144 — CLOSED** (prod retest): void no-500 (**migration 96 confirmed on the
  prod DB**) + refund reversal; `/kds` no longer logs the owner out; KDS ticket delivers + advances
  and persists across reload; bulk stock-track + threshold persist. A144 closed **for the tested
  slice only** — the remaining two write-endpoints are carried as **A197**.
- **A69 / A147 / A70 — CLOSED** (admin browser pass): enrol-code mint (verified via the **Overview**
  path); web-access-expiry row (`PATCH /clients/:id/web-access`) — the other two "unwired" endpoints
  were already-wired false positives; enrolled-device roster renders from real rows.
- **A154 — BUILT + CLOSED:** the admin DB-migrations panel was a finished-but-unwired page; added
  the backend route, mounted the page, allowlisted the runner-bootstrapped table. No schema change,
  no prod-migrate. All gates green; panel loads real data on prod.
- **A196 — BUILT + CLOSED:** voiding an order left its kitchen ticket live on `/kds` (the kitchen
  kept cooking cancelled orders). Void now pulls the ticket; refund pulls it only if not yet started
  (a `preparing`/`ready` dish is left to finish, since a refund keeps the sale on the books).
- **A198 — OPENED + BUILT + CLOSED:** the Branches-tab "Enrol till" minted a single-use code with
  **nowhere to display it** (only the Overview tab rendered the result), silently burning codes.
  Now renders the same code card on the Branches tab.
- **A151 — investigated, NOT changed (rule 17 win):** the register described a money bug ("pay loop
  never advances past guest 1"), but the code was **already rebuilt** — even-split was live-confirmed
  2026-08-31, and by-item (`ByItemSplitPanel`) feeds the same proven multi-leg `/pay` path, reconciled
  to the full order total. It needs a **by-item live test**, not a fix. Stays OPEN pending that test.
- **Opened, still OPEN:** A192 (KDS renders a 401 ticket-fetch as "all clear"), A193 (refund has no
  audit-log view), A194 (no customer-name at POS, P3), A195 (refunded orders show "Completed" with no
  badge), A197 (A144 remainder: transfer approve/complete + direct branch-stock set unwired).

## 5. Failed attempts / dead ends (so nobody re-walks them)
- **Merged ≠ live — the whole "A154/A198 don't work on prod" saga.** The admin frontend was current
  (Migrations page present, calling `/api/admin/migrations`), but the **prod server 404'd** the route
  while dev returned 401. Cause: a **failed Render deploy** had left prod on an old build. It was
  never a code bug — dev proved the code correct. A redeploy from `main` fixed the 404 and the
  "missing" enrol card together. (Earlier in the thread the same class of thing appeared as prod
  being wired to `dev` instead of `main`.)
- **A151 rebuild trap avoided.** Believing the stale register description would have meant rebuilding
  split-bill that already exists. Sweep the source first (rule 17).
- **Do NOT flip `/pay`'s leg-sum check to a 400.** `checkPaymentIntegrity` on the split-bill `/pay`
  path is **deliberately log-only** (`[payment-mismatch]`) — a documented pilot decision, because an
  enforcement bug would refuse a real sale mid-service. The takeaway create path (`POST /orders`)
  already reconciles and aborts; the `/pay` flip waits for a week of live mismatch data + owner nod.

## 6. Next / skipped / proposed
**Immediate:**
1. **PR `dev` → `main`** for the A196/A198 register close + this handoff (`dev` is 1 ahead).
2. **A151 by-item split live test** (now unblocked — A187 gives a void path): assign lines to guests,
   pay per guest, confirm the legs sum to the total and one order closes. PASS → close A151.

**Active cloud backlog (proposed, not built):**
- **A192** — `/kds` must surface a 401 as an explicit un-paired state, not "all clear".
- **A195** — a "Refunded" badge on the Orders row (keep "Completed"; do NOT relabel).
- **A193** — a refund audit-log view (money-audit; sibling of A195).
- **A197** — wire the two inventory write-endpoints (transfer approve/complete, direct set).
- **A143 / A146 / A141** — "endpoints live, no UI caller" wiring; **A133** Slice 2 (manager parity).
- **A145** — retire the redundant/under-guarded branch↔user assignment routes (owner nod).
- **A159** — flip the device write-guard to enforce (after reviewing live dry-run data).
- **A157** — wire the unused validation schemas (per-route, would 400 live requests as-is).
- **A54** — mail: set `RESEND_API_KEY` + verify a domain (blocked on owner; mailer is Resend-first).

**Parked — whole desktop/node track (needs a real till):** A17 (P0, day-15 lockout), the node/
failover cluster (A19/A20/A24 ↔ A160–A164), A158/A168/A179/A182/A18/A22/A23, and all D-items.

## 7. Note
1. **Merged ≠ live.** The single recurring failure on this project. Prod server deploys from `main`;
   the test box (`swiftpos-20c2`) from `dev`. After every deploy: `curl /health` + probe one new
   route (401 present / 404 stale). It catches a silent-failed deploy in seconds.
2. **Migration state (confirmed on prod this session):** 92, 96 applied (device-grant column present;
   void writes `voided_by` with no 500). Keep confirming migrations on the box that serves the code.
3. **The register is current and honest** — ten closes this session, every one on live evidence
   (rule 16, nothing closed on the bench). Keep it that way. Credentials are rotated per session.

## 8. Only live check outstanding from this session
Everything closed this session was verified on prod and does **not** need re-testing. The one
remaining live check is **A151 (by-item split)** — see §6.2. Hand it to a browsing agent in a
logged-in owner session on a restaurant branch:

> Open a dine-in table with ≥2 guests and several items. Use **Split Bill → by item**: assign each
> line to a guest, then pay each guest's portion. PASS if every guest's leg is collected, the legs
> sum to the **full order total**, and the order closes **once** (no orphaned balance, table freed).
> Capture any `[payment-mismatch]` behaviour or a leg that doesn't advance. Report VERDICT / PATH /
> OBSERVED / EXPECTED / EVIDENCE.
