# MANIFEST 2026-09-04-o — A209: web receipts in the desktop format (server-render core)

**Base:** stacks on the manager work. **Scope:** print-server + one web lib + a test.
**Option B (owner decision): the print-server renders** via shared/printing, so web bytes == desktop.

## Built + proven
- `apps/print-server/src/index.js` — `POST /print/receipt` renders `{ target, order, business }` with
  shared/printing `renderTicket`/`toEscPos` and sends the bytes.
- `apps/dashboard/src/lib/buildReceiptOrder.ts` — pure mapper: completed sale → the exact
  Order/BusinessConfig JSON shape (money in cents, vat/ctl rates). No rendering (browser-safe).
- `tests/receipt-escpos-format.test.mjs` — feeds a mapper-shaped Order through the real shared/printing
  render, asserts valid ESC/POS (ESC@ init, bill number, line, business name). Mutation-checked.

## Verification (rule 7)
dashboard tsc 0 · vite build 0 · format test 3/3 (skips cleanly if shared/printing isn't built) ·
check-api-routes / test-registration / root-clean green. `shared/printing` builds (dist gitignored;
the print-server already builds it to run).

## REMAINING — last-mile wiring (needs print-server config)
1. Add print-server **URL + token + printer target** to `PrinterSettings` (+ a PrinterSettingsModal
   field set). The token is generated by the print-server on first run.
2. `printReceiptViaServer(order, business, target, token)` → POST `/print/receipt` with `X-Print-Token`.
3. Wire `PaymentModal` to prefer the ESC/POS server path when configured; fall back to the current
   QZ/browser path otherwise.

## HARDWARE (owner)
The actual "test the web printer utility" — a real receipt from a real printer via the running
print-server — is owner-side. The bytes are proven identical to desktop, so the printed format matches.

## Rollback
```
git checkout <base> -- apps/print-server/src/index.js docs/AUDIT-REGISTER.md
rm apps/dashboard/src/lib/buildReceiptOrder.ts tests/receipt-escpos-format.test.mjs docs/MANIFEST-2026-09-04-o.md
```
