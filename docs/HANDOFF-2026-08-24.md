# HANDOFF — 2026-08-24 (evening)

Session focus: terminal-credential hardening (A158+A159+A160 a/b), the 0.5.36 desktop
release, and the live Render-swap / tech-token incident. Everything below is bench-green
on Linux/Node 22; target-only items are flagged OPEN pending on-till verification (rule 16).

---

## 1. Where the branches stand

- `origin/main` — **A111** (`9906e0b`), untouched. Production. Cloud auto-deploys from it.
- `origin/dev` — **`a36d1dc`** (desktop v0.5.36). CI #144 green. Carries A140–A157 + the
  A152 offline-auth fix + the version bump.
- Tag **`v0.5.36`** pushed, points at `a36d1dc`. GitHub *release* still to be cut (web UI;
  `gh` not installed — notes drafted in chat).
- **Not yet applied to dev:** batches **-g, -h, -i** (this evening's work) — patches in hand,
  see §3.

---

## 2. What shipped / was resolved today

**A152 (P0, offline auth)** — 5xx-from-cloud now falls through to node/cache (batches a–f,
already on dev). Desktop 0.5.36 built + signed + tagged. OPEN P0 until the on-till 502/503
test.

**Render swap + tech-token incident (resolved via guidance + script).** New cloud is
`https://swiftpos-20c2.onrender.com` (same Supabase). Tech menu broke because the tech
token is Ed25519-signed and the new server had a different/missing keypair. Fix: set stable
`TECH_SIGNING_PRIVATE_KEY` + `TECH_SIGNING_PUBLIC_KEY` + `NODE_ENV=production` on the new
Render, redeploy; then re-point each till's `server_url`. Delivered `repoint-till.mjs` +
`repoint-till.bat` (node:sqlite, backs up, idempotent, tested). Tills re-cache the new
public key on owner login.

**A158 (P1) — batch -g — owner login REMOVED from tills.** Enrolment code is the sole
activation; owner email/password never touches a shared till. `App.tsx` enrol-state +
`EnrolPage`, `LoginPage` deleted, `auth:login` IPC/preload/posApi removed, `/desktop-login`
tombstoned (410). Sign-out clears staff only (device stays enrolled). New
`terminal-activation.test.mjs`. OPEN P1 pending amber-build check.

**A159 (P2) — batch -h — stolen-token write guard (dry-run).** A `surface:'desktop'` token
can't write dashboard data (products/prices/users/settings). Default-deny by surface + a
5-entry till allowlist, inside `requireAuth`. **Dry-run by default** (`TERMINAL_WRITE_ENFORCE`
unset = log-only) so it can't break sync. New `terminal-write-guard.test.mjs`. OPEN P2
pending enforce-flip + verification.

**A160 (P1) — batch -i — node brokers token refresh (parts a+b).** An offline peer refreshes
through its node instead of falling to a login. New `POST /node/refresh` (X-Node-Secret auth)
proxies the peer's refresh token to the cloud; `syncEngine` falls back to the node on
cloud-unreachable/5xx (A152 pattern), never on a 401 (revoked stays final). No migration —
the refresh token is the device credential. New `node-token-refresh.test.mjs`. OPEN P1
pending the two-till drill.

---

## 3. Apply order for -g / -h / -i (on the dev tip a36d1dc)

Patches are in the outputs folder. Commit sequence in §7.
```
-g  A158  retire owner-login (enrolment-only)
-h  A159  terminal write guard (dry-run)
-i  A160  node-brokered token refresh (a+b)
```
Each was clean-room-verified to apply on the previous. No lockfile, no version bump, no
migration in any of them.

---

## 4. OPEN items with close conditions (all target-side)

| Item | Close condition |
|------|-----------------|
| **A152** P0 | On a till, point at a 502/503 (or stop Render): cached cashier signs in, wrong PIN refused, owner screen shows outage msg. |
| **A158** P1 | Amber build: fresh till → enrol → PIN (no login screen); wipe `session` row → EnrolPage; sign-out → PIN, device stays enrolled. |
| **A159** P2 | Run dry-run a few days, watch `[terminal-write-guard] DRY-RUN … would block` logs, add any legit till write to the allowlist, then `TERMINAL_WRITE_ENFORCE=true`; confirm a crafted desktop POST /api/products → 403 and sales/sync unaffected. |
| **A160** P1 | Two tills + node: cut the peer's cloud (node stays online), let the peer's access token lapse, confirm it refreshes via the node and keeps selling; a revoked token still ends the session. |

---

## 5. Standing carryovers (not code — owner/ops)

- **ROTATE THE PAT** — shared in chat, still live. Do this first.
- **GitHub release for v0.5.36** — web UI, attach `SwiftPOS-0.5.36-x64.exe` + `-portable.exe`.
- **Rollout order (rule 13):** update ALL client tills to the enrolment-only build BEFORE the
  `/desktop-login` tombstone reaches their server, or an un-updated till loses its only sign-in.
- **MAIN production deploy:** run the `DB migrate (production)` Action for migrations **89, 90,
  91** FIRST (A128/A129/A139 money-path fixes expect the widened domains), resolve the twin-`90`
  filename collision (keep `90_order_type_delivery_check.sql`, retire/renumber
  `90_restore_order_types.sql`), THEN promote dev→main. Migrate-then-promote, never the reverse.

---

## 6. Node-authority (A160) — Phase (c), the last mile

Parts (a)+(b) shipped. Phase (c) — "only the node online" (node mints its own tokens; peers
never touch the cloud) — is **blocked on three unbuilt pieces** and must be built in order:
**A19** (node forwards peer sales to the cloud) → **A24** (node serves reference data down;
also fix `business_settings.branch_id` + the dual kitchen-exclusion sources) → **A20** (roster
replication so the node authenticates its devices) → then the node-mint. All are source-mapped
in the register; full design in `SCOPE-node-authority-A160.md`. Do NOT build the node-minter
before A19/A24 — it's dead code without the uplink and the reference channel.

---

## 7. Commit + push (pick up tomorrow)

See the chat message for the exact block. In short, from `/c/swiftpos/pos` on `dev`: apply
-g/-h/-i in order, commit each, `run-all.mjs` green, push. Then verify on the amber build.

## Working rules still in force
`HANDOFF-2026-08-08-evening.md` §0 rules 1–23. Key ones this session: additive during deploy
windows (r13); nothing ships without a register ID (r14); say "node"/"cloud" not "server"
(r21); target-only items stay OPEN pending on-till verification (r16); if a fix grows, stop
and ask (r12).
