# HANDOFF — 2026-09-25 (full day; owner present throughout)

**Read first:** `docs/WORKING-METHOD.md` (§9 gained two rules today), the rules in `HANDOFF-2026-08-08-evening.md` §0, and the
previous handoff `HANDOFF-2026-09-24.md`. Standing rulings unchanged (zip + manifest for every delivery; rule 21 in UI strings; no
reference business anywhere).

## Headline
- **Branding Phase 2 (A323): every build slice closed on target** — the till (A326), the web picker (A327) and the web POS + shared
  components (A328) all follow the client's theme; brand colour proven on the till. Remaining: **slice 5** (owner's on-till walk).
- **Owner decision: SwiftPOS's own colour is now TEAL (A329)** — the logo's colour; green now means success only; 2× farther from
  M-Pesa green. **Step 2 shipped:** tills (**v0.6.7**, Release #23) + web POS default to teal. **Step 3 (back office) swept, not built.**
- Lead-dev decision (delegated): **the back office stays SwiftPOS-branded** — never the client theme; its greens → teal (A329 step 3).
- Defects found and fixed: A331 (themed tills: white labels on 600 < 4.5 for 5 themes) · A330 (11 translucent grey panels dark in
  light mode) · A328 part 2 (the web POS's inline colours were invisible to a class-only sweep — the owner spotted it).
- Open: **0 P0 · 18 P1 · 19 P2 · 19 P3**. Tip after this delivery: see below; desktop **v0.6.7**.

## Deliveries today
| Delivery | Commit | CI | What |
|---|---|---|---|
| -a | `352dded` | green | A328 part 1: web POS panels + shared components (Tailwind classes); A326 + A327 closed |
| -b | `9d07e57` | #400 | A328 part 2: the web POS's 94 inline colours (hex/rgba) themed; gate sees hex |
| -c | `6181bc0` | #401 | Docs: A328 closed; 4b-2 decision; A329 opened; A330 opened; slice 5 checklist |
| -d | `2c163d5` | #402 | A330: 11 light-mode rules for translucent grey panels; guard test |
| -e | `ba5aeef` | #403 · Release #23 | A329 step 2 (teal defaults on tills + web POS, wordmark) + A331; **v0.6.7** |
| -f | (this) | — | Test labels; step 3 sweep recorded; this handoff |

## Next session — in this order
1. **A329 step 3 — the back office → SwiftPOS teal.** Sweep recorded in A329 (641 uses, 59 of 104 files; first-pass: action 243,
   status 89, money 27, data 126, review 156 in ~114 patterns). Re-run the sweep (all forms, every file listed), decide the patterns,
   **audit the auto-actions** (status → action direction), owner reviews the document, then a FIXED `swift-*` token set (not the
   client theme), script-applied, ratchet, verified dark + light. Probably 2 deliveries.
2. **Slice 5 walk** (owner) — `docs/VERIFY-BRANDING-PHASE2.md`; **B1** (Teal vs "paid" in shop light) is also A329's shade check.
3. **A329 step 5** — a short heads-up to clients that SwiftPOS moved from green to teal.
4. Owner checks still open: 0.6.7 on the till with themes OFF (teal) — and dashboard deployed from `ba5aeef`.

## Follow-ups logged today
- Light-mode `hover:` overrides inside the Tailwind layer are dropped by Tailwind (A330 note) — cosmetic.
- 56 older tests still end with `process.exit` (WORKING-METHOD §9) · node bundle relays no branding/theme (A325 note).
- A320 / A321 / A318 siblings (from 2026-09-23) still open.

## Lessons added today (WORKING-METHOD §9)
A sweep must match every colour form the code uses (classes, hex, rgba) and list every file scanned — a key screen with 0 hits is a
red flag · check dashboard UI in dark AND light (dashboard is dark-first) · new tests end with `process.exitCode`.
