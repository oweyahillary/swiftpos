# HANDOFF — 2026-09-02 (cloud/dashboard session)

Working rules are unchanged — see **HANDOFF-2026-08-08-evening.md §0** (the 24 standing
rules) and the environment note. The register (`docs/AUDIT-REGISTER.md`) is the source of
truth and is fully up to date as of this handoff.

> **⚠️ READ FIRST — before doing ANY work, run the test prompt in §8.** Most of the pain
> this session came from testing/building against a **stale deployment** and from a
> **register that had drifted behind reality**. Confirm what is actually deployed first.

---

## 1. Goal
Continue SwiftPOS on the **cloud/dashboard + server** side (desktop is parked — see §6),
closing open register items: the POS re-skin, an owner Orders view with void/refund, the
KDS kitchen display, terminal identity, permission/inventory wiring, and the mail pipeline.

## 2. Current state
- **Branch `dev`**, last known good HEAD before this session's final fix: `fba45bb`. The
  final fix (void-500 + /kds logout + A144 + A3) ships as
  `swiftpos-2026-09-02-void500-and-kds-logout-fix.zip` and must be committed **and a file
  deleted** (`git rm apps/dashboard/src/lib/kdsRealtime.ts`) **and migration 96 applied**.
- **All code is on/going-to-`dev`.** Nothing is half-written. The remaining blockers are
  **ops**, not code: (a) redeploy the dashboard+server, (b) run migration 96, (c) set
  `RESEND_API_KEY` + verify a domain (mail).
- **Counts:** A: 1 P0 · 20 P1 · 18 P2 · 7 P3 — D: 1 P0 · 2 P1 · 2 P2 · 3 P3. But the P0 and
  most P1s are **parked desktop** items; the *active* cloud backlog is small (§6).
- **Confirmed working after the last redeploy (2026-09-02 retest):** A190 (7 tiles/row),
  A184 (terminal rename). **Fixed but needs a retest after deploy+migrate:** A187 void,
  A191 /kds logout, A3 ticket delivery, A144 tracking, A146 mail (config).

## 3. Active files (touched this session)
Dashboard:
- `apps/dashboard/src/pages/pos/CashierScreen.tsx` — A185 re-skin, A188 grid fallback, A190 tile density (100px)
- `apps/dashboard/src/pages/OrdersPage.tsx` — A187 Orders view + void/refund + owner-void-anytime + closed-period warning
- `apps/dashboard/src/pages/kds/KDSPage.tsx` — A3 token setup; A191 **realtime removed**, 10s poll
- `apps/dashboard/src/pages/settings/KitchenDisplayTab.tsx` — A3 owner KDS-token generator (NEW)
- `apps/dashboard/src/pages/FleetPage.tsx` — A184 inline terminal rename
- `apps/dashboard/src/pages/settings/ReportSchedulerTab.tsx` — A146 "Send test email" button
- `apps/dashboard/src/pages/pos/POSInventoryTab.tsx` — A144 threshold editor
- `apps/dashboard/src/pages/products/ProductsPage.tsx` — A144 bulk Track/Untrack
- `apps/dashboard/src/App.tsx`, `components/DashboardLayout.tsx` — routing/nav (Orders, Kitchen display)
- **DELETED:** `apps/dashboard/src/lib/kdsRealtime.ts` (A191 — was a dead end)

Server:
- `apps/server/src/routes/orders.ts` — A187 owner self-authorised void/refund; owner-void-anytime window bypass
- `apps/server/src/routes/kitchen.ts` — A3 kds-token mint/accept; **tickets query fix** (dropped fragile nested filter)
- `apps/server/src/middleware/auth.ts` — A3 confine `surface:'kds'` tokens to the kitchen router
- `apps/server/src/routes/webhooks.ts` — A146 log test-ping deliveries
- `apps/server/src/routes/products.ts` — A144 `PATCH /products/bulk-track/by-ids`
- `migrations/95_kitchen_tickets_realtime.sql`, `migrations/96_orders_voided_by_fk.sql` — **both PROD-MIGRATE**

Tests / docs:
- `tests/owner-void-refund.test.mjs`, `tests/kds-token.test.mjs`, `tests/webhook-test-logs.test.mjs` (guard tests, mutation-checked)
- `docs/E2E-CI-PLAN.md`, `e2e/seed/seed-business.mjs` (A189, parked)

## 4. Changes made (by ID)
- **A185/A188/A190** — POS re-skin to the desktop palette; tables render as a grid; tiles ~7–8/row. (A190 confirmed PASS.)
- **A187** — owner **Orders** page (view) + **void/refund** (server self-authorises owner, no PIN, audit trail kept); **owner-void-anytime** (staff stay 30-min-limited) + closed-period warning; **void-500 fixed** (migration 96: `voided_by` FK → `public.users`).
- **A3** — per-branch **KDS token** (mint/accept/confine) + `/kds` one-time paste setup + owner generator; **tickets query fix** (fragile 2-level embed filter → JS filter); migration 95 (realtime publication, now moot).
- **A184** — inline **terminal rename** on the Terminals screen. (Confirmed PASS.)
- **A146** — test-ping **delivery logging** + **"Send test email"** button; diagnosed the mail failure as a **Render outbound-SMTP block** (the A50/A54 root cause). Mailer already supports Resend (HTTP) — needs `RESEND_API_KEY`.
- **A144** — product **low-stock threshold** editor (POS Inventory tab) + **bulk Track/Untrack** for existing products.
- **A191** — `/kds` was logging the owner out app-wide; **removed the realtime subscription** so `/kds` touches no Supabase client.
- **Register reconciliation** — closed **A149** (CI covers admin), downgraded **A186** (CI-mitigated), corrected **A141** (permission-backfill gap, not a bug), and confirmed many items were **already built** (A151, A140, A165, A166, A130, A8).

## 5. Failed attempts / dead ends (so nobody re-walks them)
- **A191 — two wrong fixes before the right one.** (1) "Deploying the A3 /kds rewrite will fix it" — no. (2) "Isolate KDS realtime onto a separate `kdsRealtime` client" — no (reproduced a 6th time). The real cause: `ProtectedRoute` gates on the Supabase **GoTrue session**, and *any* Supabase-client activity on `/kds` can invalidate it. Fix = remove realtime from `/kds` entirely (poll only).
- **Deploy lag, repeatedly.** Three batches were pushed to the repo but **never reached the deployed build** (A187 Phase-2b, A184, A189 docs) → agents reported features "absent" that were actually committed. A `git log` / `check-doc-refs` after each push would have caught every one.
- **Rule-17 misses (features that already existed).** A151 split-bill, A140/A165 menu import, A184 rename endpoint + telemetry, A144's per-product track path (row **Edit**), and the **Resend HTTP mailer** were all already built — the real problems were register drift, deploy lag, and the test agent not finding existing UI. Always sweep the source before building.
- **A3 ticket delivery — wrong first theory.** Guessed the test order (cash takeaway) made no kitchen ticket; the code creates one for *every* order. The bug was the read query, not the order type.

## 6. Next / skipped / proposed
**Immediate (ops — do these first):**
1. Apply the final zip, `git rm apps/dashboard/src/lib/kdsRealtime.ts`, run **migration 96** (and 95 if not run), commit, push.
2. **Redeploy dashboard + server** from current `dev`.
3. **Mail:** set `RESEND_API_KEY` + verify a sending domain in Render, set `NOTIFY_FROM_EMAIL` to an address on it. Closes **A50/A54/A146**. (No code needed — the mailer is Resend-first already.)
4. Run the §8 test prompt.

**Proposed (not built):**
- **A141** — permission backfill: grant newly-added default permissions (e.g. `ingredients.manage`) to roles created before the key existed. B Fastfoods' roles predate it, so the CTA can't appear without this.
- **A144** — the other two endpoints (transfer approve/complete, direct branch-stock set) are still unwired.
- **A143** — remaining report exports + an inventory report tab (needs a mapping decision).
- **A133** — Settings Slice 2 (manager-side parity).

**Parked:**
- **A189** — E2E-in-CI: seed built (`e2e/seed/seed-business.mjs`), plan in `docs/E2E-CI-PLAN.md`; resume at VPS setup or customer #2.
- **True KDS realtime** — needs a Supabase-compatible token for the anon subscriber under RLS; the 10s poll is functional meanwhile.
- **Whole desktop track** — awaiting the desktop test report: A17 (P0, day-15 lockout), the node/failover cluster (A19/A20/A24 ↔ A160–A164), A158/A168/A179/A182/A18/A22/A23, and all D-items (D1 P0, D3/D4, D7/D18, D9/D10/D17).

## 7. Note
Two structural truths surfaced this session and should shape the next one:
1. **Deploy is the bottleneck, not code.** Confirm the deployed build's commit before
   testing or building on top. One redeploy + the Resend key would flip most of the open
   cloud board green.
2. **The register had drifted; it's now caught up.** Keep it that way — it repeatedly saved
   us from rebuilding things that already existed. Credentials are rotated per session.

## 8. Test prompt — RUN THIS FIRST (after deploy + migrate + Resend key)

> Hand this to a browsing agent in a logged-in **owner** session on the live app.
> Return the report as: `| ID | Verdict | one-line |` + a per-ID block
> (VERDICT / PATH / OBSERVED / EXPECTED / EVIDENCE / NOTES). Verdicts: PASS / FAIL /
> PARTIAL / BLOCKED. Real business — for anything that writes in bulk, preview-then-cancel;
> void a test order you create afterwards.

**STEP 0 — DEPLOY GATE (stop if it fails).** Finance → Orders → an order **>30 min old** →
does it show a **Void** button (red)? If only Refund → the deploy is stale; STOP and report
"deploy stale".

1. **A187** — On an old order click **Void** → confirm the amber closed-period warning +
   reason field, submit → the order becomes **voided with NO 500** (the fix), reason + your
   name recorded. Confirm Refund still works too.
2. **A191** — Open **`/kds`** (paste a KDS token from Settings → Devices → Kitchen display →
   Generate, if asked), let it sit ~30s, then go back to a dashboard tab → **still logged in**
   (no redirect to /login).
3. **A3** — Ring a normal order for the KDS branch (pay-and-go is fine) → the ticket appears
   on `/kds` within **~10s**. Advance it (Start → Ready → Collected).
4. **A144** — Products → select a few untracked products → **Track stock** → they appear in
   the POS Inventory tab → edit a **min** threshold → it persists on reload.
5. **A146** — Settings → Business → Report scheduler → **Send test email** → it **arrives**
   (this only works once `RESEND_API_KEY` + a verified domain are set).
6. **A184** — Settings → Devices → Terminals → hover a row → ✎ → rename → persists on reload.
   (Already PASSed once; re-confirm.)

Report which pass. PASS → close the item in the register. FAIL on A187/A191/A3 → capture the
exact server 500 / console error / network request, as those are the three schema/Supabase
fixes verified only by this live run.
