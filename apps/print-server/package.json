{
  "name": "swiftpos-print-bridge",
  "version": "2.0.0",
  "description": "Local print bridge for the SwiftPOS web POS. Zero dependencies.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build:win": "node --experimental-sea-config sea-config.json"
  },
  "dependencies": {},
  "engines": { "node": ">=20" }
}
